import React from 'react';
import { Button, MenuIcon } from './MenuButton.styled';

const MenuButton = () => {
  return (
    <div>
      <Button>
        <MenuIcon />
      </Button>
    </div>
  );
};

export default MenuButton;
